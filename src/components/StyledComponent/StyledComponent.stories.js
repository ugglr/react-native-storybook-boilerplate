import React from 'react'
import { storiesOf } from '@storybook/react-native'

import StyledComponent from './StyledComponent'

storiesOf('Styled Component', module).add('example', () => <StyledComponent />)
